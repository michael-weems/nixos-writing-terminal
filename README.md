# To use

- create an `rclone` configuration using the command `rclone config`
- put that configuration under `home/.config/rclone/rclone.conf`
- configure your nixos system
- manually copy the `rclone.conf` file to:
    - `/root/.config/rclone/rclone.conf`
    - `/etc/rclone-mnt.conf`
